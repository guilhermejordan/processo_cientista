# Este projeto usa o pacote checkpoint para o controle de pacotes
# Para reproduzir o ambiente utilizado, instale o pacote checkpoint
# install.packages('checkpoint')
# e execute este script
library(checkpoint)
checkpoint('2020-01-14')
